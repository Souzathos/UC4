// Crio a interface usuário
// Qualquer objeto ou classe que implemente/use esta interace PRECISA seguir as regras dela, ou seja, ter todos os seus atributos, tipos e métodos (se houver)
export interface IUsuario {
    nome: string
    idade: number
}